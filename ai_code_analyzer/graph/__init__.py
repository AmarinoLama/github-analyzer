"""Capa de orquestación con LangGraph.

* `state`     -> define el estado que fluye entre nodos.
* `nodes`     -> implementa cada nodo (una función Python normal).
* `workflows` -> ensambla los grafos (nodos + transiciones).
"""
